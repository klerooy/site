services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: artistic-backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/api/health', timeout=2)"]
      interval: 10s
      timeout: 3s
      retries: 10
      start_period: 10s

  frontend:
      build:
        context: ./frontend
        dockerfile: Dockerfile.dev
      container_name: artistic-frontend
      restart: always
      ports:
        - "5173:5173" # Порт Vite
      volumes:
        - ./frontend:/app # <-- ЭТО САМОЕ ВАЖНОЕ: пробрасываем ваши файлы внутрь
        - /app/node_modules # Исключаем node_modules, чтобы использовать те, что внутри контейнера
      environment:
        - CHOKIDAR_USEPOLLING=true # Помогает отслеживать изменения файлов
